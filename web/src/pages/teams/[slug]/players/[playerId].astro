---
// Individual player pages depend on Team_Members data, which isn't wired up
// yet — no pages get generated until that's in place. The Roster section on
// the team page links here once real rosters exist.
export function getStaticPaths() {
  return [];
}
---
